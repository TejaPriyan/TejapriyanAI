import ChatApp from "@/components/ChatApp";

export const metadata = { title: "Chat · Teja Priyan AI" };

/** Full-screen chat workspace. */
export default function ChatPage() {
  return <ChatApp />;
}
